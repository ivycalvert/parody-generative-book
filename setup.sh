# install required libraries

# install the required libraries not already on the class setup
pip install pronouncing
pip install captcha

# make the directory for the CAPTCHA images
mkdir -p CAPTCHAs